# 检查 commit message 格式
commit_msg_file="$1"
commit_msg=$(cat "$commit_msg_file")

# 基本要求：commit message 不能为空
if [ -z "$commit_msg" ]; then
    echo "❌ Commit message cannot be empty."
    exit 1
fi

echo "✅ Commit message check passed."
