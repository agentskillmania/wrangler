name: example-test
description: An example test case demonstrating the YAML test format

input:
  message: Say hello and write the greeting to hello.txt

context:
  mode: agent

expected:
  hard:
    - type: output_contains
      value: "hello"
    - type: file_exists
      path: "hello.txt"
