---
"@agentskillmania/wrangler": patch
---

MarkdownMessageAssembler no longer injects active skill into the dynamic reminder (skill instructions now persist in conversation history as load_skill tool results, handled by colts).
